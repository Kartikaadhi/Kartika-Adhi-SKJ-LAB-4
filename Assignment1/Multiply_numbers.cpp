#include <iostream>

int main() {
    // Define variables
    int num1 = 5;
    int num2 = 10;
    int result = 0;

    // Perform multiplication
    result = num1 * num2;

    // Output the result (simulating storing the result)
    std::cout << "Result: " << result << std::endl;

    // Exit the program
    return 0;
}
